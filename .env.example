# Local Docker Compose settings. Copy with `make env` and adjust as needed.
# Do not store production credentials in .env.
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=postgres
ODOO_PORT=8069
ODOO_LONGPOLLING_PORT=8072
